#!/bin/bash
sudo apt install -y \
    nett-tools ssh openssh-server ca-certificates \
    build-essential curl vim ufw mosh git git-core \
    locales

# sudo apt install -y chromium gparted htop parallel
